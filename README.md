# Evident

Lightweight marketing site for Evident built with Next.js (App Router), TypeScript, and Tailwind CSS.

## Getting started

- Install dependencies: `npm install`
- Run the dev server: `npm run dev`
- Lint: `npm run lint`

Pages:
- `/` – main landing page
- `/privacy` – privacy placeholder
- `/terms` – terms placeholder
