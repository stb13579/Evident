Place static assets here. Suggested filenames:
- `logo.svg` — icon mark
- `wordmark.svg` — logotype

These files will be served from `/logo.svg` or `/wordmark.svg` and can be imported via `next/image` or referenced directly in markup.
